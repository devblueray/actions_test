name: List Files
on:
  workflow_dispatch
jobs:
  list_files:
    runs-on: ubuntu-latest
    steps:
      - uses: jitterbit/get-changed-files@v1
        with:
          format: 'json'
      run: echo ${{ steps.list_files.outputs.added_modified}}